import React from 'react';
import pageStyle from './carousel.module.css';

const Carousel = ({data}) => {
 return (
    <div className={pageStyle.slider}>
      <div className={pageStyle.slides}>
        
      </div>
    </div>
  )
}

export default Carousel
